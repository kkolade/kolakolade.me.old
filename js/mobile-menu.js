// // contactEmail.addEventListener("input", (e) => {
// //   if (!contactEmail.value.match(regEx)) {
// //     err.className = "error";
// //     displayError();
// //   } else {
// //     err.textContent = "Play";
// //     // displayError();
// //   }
// // });

// // contactForm.addEventListener("submit", (e) => {
// //   if (contactEmail.value.match(regEx)) {
// //     e.preventDefault();
// //     displayError();
// //   }
// // });

// function displayError() {
//   if (!contactEmail.value.match(regEx)) {
//     err.textContent = "Input must be all lowercase";
//   }

//   // else {
//   //   emailError.textContent = "Input must be all lowercase";

//   // }

// contactSubmit.addEventListener('click', validateEmail);
